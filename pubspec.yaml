name: xsd
description: Strict, lossless, and platform-consistent implementations of W3C XSD 1.1 Datatypes for Dart.
version: 0.1.0
repository: https://github.com/dropbear-software/xsd

environment:
  sdk: ^3.10.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
